node_modules/
target/
dist/
/bin/
fixtures/*.mp4
fixtures/*.mp3
fixtures/*.png
fixtures/*.wav
# Кроме голосовой: её нельзя пересоздать где угодно. Остальные фикстуры строит
# ffmpeg из lavfi, то есть одинаково и всегда, а речь требует синтезатора,
# которого на раннере может не оказаться — см. комментарий в fixtures/make.sh.
!fixtures/speech.wav
fixtures/*.aiff
fixtures/smoke-out/
.DS_Store
dist-release/
# Sidecar engines (ffmpeg/ffprobe/whisper-cli, ~130MB): put there by
# scripts/fetch-sidecars.sh from pinned sources, never committed.
app/src-tauri/binaries/

# артефакты браузерных проверок
.playwright-mcp/
