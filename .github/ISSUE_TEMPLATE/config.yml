# Пустой issue оставлен включённым намеренно: кнопки в самом приложении ведут на
# `issues/new?body=…` с уже подставленной версией и системой, а этот адрес
# работает только пока пустые issue разрешены. Отключить их — значит сломать
# единственную кнопку обратной связи внутри программы.
blank_issues_enabled: true
